[pytest]
pythonpath = applications
python_files = test_*
python_classes = *Tests
python_functions = test_*
addopts = -p no:warnings

markers =
    log_in: Tests when logged in
    log_out: Tests when not logged in
    home_page: Tests on '/'
    auth: Tests on '/auth'

